package model

import org.scalatest.{FlatSpec, Matchers}

/**
  * Created by wojciech on 07.04.17. 
  */
class PwoXYSpec extends FlatSpec with Matchers {

//  val function: (VectorXY) => Double = (v: VectorXY) =>
//    v.x * v.x + v.y * v.y - 20 * (math.cos(math.Pi * v.x) + math.cos(math.Pi * v.y) - 2)
//
//  val instance = new PwoXY(function, _ < _, 3.0)
//
//  val x: List[(VectorXY, VectorXY)] = VectorXY.randomRange(-10, 10, -10, 10)
//    .zip(Iterator.continually(VectorXY(0,0))).take(40).toList
//
//  instance.start(x, 2.0, 2.0).foreach{iterator =>
//    iterator.take(2).foreach(println)
//
//  }



}
